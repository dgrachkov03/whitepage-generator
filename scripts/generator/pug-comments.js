export const BLOCK_APPEARANCE_INTRO = [
  "    // blockAppearance — оформление секций.",
  "    // Чередуйте tone: base и alternate у соседних блоков для контраста.",
  "",
  "    // tone — фон секции: base (основной) | alternate (контрастный) | primary (брендовый) | transparent (без заливки)",
  "    // divider — линия на границе: top | bottom | both (по умолчанию none)",
  "    // surface — заливка поверх tone: inherit (из tone) | transparent | solid",
  "    // pattern — декор на фоне: none | subtle-grid | dots | diamond",
  "",
  "    // tone по умолчанию base (фон секции всё равно будет, если не указан).",
  "    // divider, surface и pattern, если не указаны, используют значения по умолчанию без доп. оформления.",
];

export const LEGAL_BLOCK_APPEARANCE_INTRO = [
  "    // blockAppearance — оформление header, document и footer на legal-страницах.",
  "",
  "    // tone — фон секции: base (основной) | alternate (контрастный) | primary (брендовый) | transparent (без заливки)",
  "    // divider — линия на границе: top | bottom | both (по умолчанию none)",
  "    // surface — заливка поверх tone: inherit (из tone) | transparent | solid",
  "    // pattern — декор на фоне: none | subtle-grid | dots | diamond",
  "",
  "    // tone по умолчанию base (фон секции всё равно будет, если не указан).",
  "    // divider, surface и pattern, если не указаны, используют значения по умолчанию без доп. оформления.",
];
